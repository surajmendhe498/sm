const arr = [1, 2, 3, 4, 5];
const sliced = arr.slice(1, 3); // Extract elements from index 1 to 2
console.log(sliced); // [2, 3]
console.log(arr);    // [1, 2, 3, 4, 5] (unchanged)


// slice() method return the selected elements in an array.
